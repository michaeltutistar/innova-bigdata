# ===========================================
# Producción AWS EC2 + RDS
# ===========================================
# Copiar a .env en el servidor: cp .env.aws.example .env
# Luego editar: nano .env (sustituir placeholders)

# ----- RDS PostgreSQL -----
# Sustituir TU_PASSWORD_RDS por la contraseña real de RDS
DATABASE_URL=postgresql://postgres:TU_PASSWORD_RDS@proyecto-innova-postgres.cduyc0g8khr3.us-east-2.rds.amazonaws.com:5432/innovabigdata?sslmode=require

# Para respaldos desde EC2 (mismos valores que en DATABASE_URL)
RDS_HOST=proyecto-innova-postgres.cduyc0g8khr3.us-east-2.rds.amazonaws.com
RDS_USER=postgres
# Si la contraseña tiene #, $ u otros caracteres especiales, usar COMILLAS SIMPLES (no dobles)
# para que no se interpreten variables. Ejemplo: RDS_PASSWORD='tu_password_aqui'
RDS_PASSWORD=TU_PASSWORD_RDS
RDS_DB=innovabigdata

# ----- Seguridad Backend (JWT) -----
# Generar una clave larga aleatoria, por ejemplo: openssl rand -base64 32
SECRET_KEY=openssl rand -hex 32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=240

# ----- API Verifik -----
VERIFIK_API_URL=https://api.verifik.co/v2/co/registraduria/votacion
VERIFIK_TOKEN=tu_token_verifik_real

# ----- CORS -----
# Dominio(s) permitidos, separados por coma. Si usas solo IP por ahora: http://IP_EC2,http://IP_EC2:80
CORS_ORIGINS=https://innovabigdata.com,https://www.innovabigdata.com,http://18.222.118.226

# ----- Servidor -----
HOST=0.0.0.0
PORT=8000

# ----- Recuperación de contraseña por email (Gmail) -----
# Cuenta desde la que se envían los correos; App Password desde Google Cuenta > Seguridad > Contraseñas de aplicaciones
GMAIL_USER=bigdatainnova@gmail.com
GMAIL_APP_PASSWORD=tu_app_password_16_caracteres
RESET_PASSWORD_BASE_URL=https://www.innovabigdata.com
